<svg xmlns="http://www.w3.org/2000/svg" width="512" height="512" viewBox="0 0 512 512">
  <!-- Fondo degradado -->
  <defs>
    <linearGradient id="gradLogin" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" style="stop-color:#667eea;stop-opacity:1" />
      <stop offset="100%" style="stop-color:#764ba2;stop-opacity:1" />
    </linearGradient>
  </defs>

  <!-- Fondo que rellena todo -->
  <rect width="512" height="512" fill="url(#gradLogin)"/>

  <!-- Regalo (gift box) -->
  <!-- Caja principal -->
  <rect x="120" y="200" width="272" height="200" fill="#fff" opacity="0.95" rx="10"/>

  <!-- Lazo horizontal -->
  <rect x="120" y="270" width="272" height="40" fill="#ffd700" opacity="0.9"/>

  <!-- Lazo vertical -->
  <rect x="236" y="200" width="40" height="200" fill="#ffd700" opacity="0.9"/>

  <!-- Tapa del regalo -->
  <rect x="100" y="160" width="312" height="50" fill="#fff" opacity="0.95" rx="8"/>
  <rect x="100" y="170" width="312" height="25" fill="#ffd700" opacity="0.9"/>

  <!-- Moño decorativo arriba -->
  <ellipse cx="200" cy="150" rx="35" ry="25" fill="#ffd700" opacity="0.9"/>
  <ellipse cx="312" cy="150" rx="35" ry="25" fill="#ffd700" opacity="0.9"/>
  <circle cx="256" cy="135" r="18" fill="#ffd700" opacity="0.9"/>

  <!-- Texto abajo -->
  <text x="256" y="475" font-family="Arial, sans-serif" font-size="48" fill="#fff" text-anchor="middle" font-weight="bold">
    Aniversaris
  </text>
</svg>
